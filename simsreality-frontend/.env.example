# 백엔드 오리진 — 모든 API 는 이 오리진 + /api/... 전체 경로로 호출 (로컬 BE 기준)
VITE_BACKEND_URL=http://localhost:8283
# 유저 대시보드(다른 팀) 주소 — 미설정 시 https://digital-twin.p-e.kr
# VITE_USER_DASHBOARD_URL=http://localhost:3000
